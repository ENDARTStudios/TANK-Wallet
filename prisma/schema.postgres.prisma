generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Re-declare for postgres (see schema.prisma for sqlite version)
// This file is informational; actual schema lives in schema.prisma
