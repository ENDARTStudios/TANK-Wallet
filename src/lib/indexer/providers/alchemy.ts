export async function fetchAlchemyTransfers(chain: string, address: string, signal?: AbortSignal): Promise<{ hash: string }[]> {
  void chain;
  void address;
  void signal;
  return [{ hash: "0xalchemy_mock" }];
}
